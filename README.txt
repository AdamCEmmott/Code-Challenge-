README:

Run main class.
Input size of words.
Input letters to use.

system outputs wordssquares and asks user if they would like definitions for those words.

if yes returns definitions from api then ends.

if no then ends.